var ancestry = require("./ancestry.js");

var array = ancestry.map(function(Math.ceil(person.died / 100) {
  return person.died - person.born;
});

for (person in array) {
  console.log(average(array[person]);
}
